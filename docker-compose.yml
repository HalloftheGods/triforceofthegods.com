version: '3.8'

services:
  triforceofthegods:
    build: .
    ports:
      - "3002:3000"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0
      - PORT=3000
    restart: unless-stopped
    container_name: triforceofthegods
